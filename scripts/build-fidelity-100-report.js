#!/usr/bin/env node
const fs=require('fs'), path=require('path'); const T=require('./fidelity-100-tools');
const root=T.root, repo='https://github.com/alexzadclaw-ai/page-profit-check-prototypes', branch='main'; const rawBase=`https://raw.githubusercontent.com/alexzadclaw-ai/page-profit-check-prototypes/${branch}`; const pagesBase='https://alexzadclaw-ai.github.io/page-profit-check-prototypes';
function raw(p){return `${rawBase}/${p}`} function blob(p){return `${repo}/blob/${branch}/${p}`} function page(p){return `${pagesBase}/${p}`}
function usage(){console.error('Usage: node scripts/build-fidelity-100-report.js <out.html> --slugs-file file | --all'); process.exit(1)}
const out=process.argv[2]; if(!out) usage(); const all=process.argv.includes('--all'); let items=T.getItems(); if(!all){const i=process.argv.indexOf('--slugs-file'); if(i<0) usage(); const wanted=new Set(fs.readFileSync(process.argv[i+1],'utf8').trim().split(/\s+/)); items=items.filter(x=>wanted.has(`${x.batch}/${x.slug}`)||wanted.has(x.slug));}
const reportId=path.basename(out,'.html'); const assetDir=`reports/assets/${reportId}`; fs.mkdirSync(path.join(root,assetDir),{recursive:true});
const {spawnSync}=require('child_process'); function makeAsset(srcRel,dstRel){const src=path.join(root,srcRel), dst=path.join(root,dstRel); fs.mkdirSync(path.dirname(dst),{recursive:true}); const py=`from PIL import Image\nimport sys\nim=Image.open(sys.argv[1]).convert('RGB')\nW,H=im.size\n# keep top 1800px, scale to 900w\nim=im.crop((0,0,W,min(H,1800)))\nim.thumbnail((900,1125))\nim.save(sys.argv[2], quality=76, method=6)`; const r=spawnSync('python3',['-c',py,src,dst],{encoding:'utf8'}); if(r.status) throw new Error(r.stderr||`asset fail ${srcRel}`)}
let cards=[]; for(const it of items){const batch=it.batch, slug=it.slug; const target=`screenshots/${batch}/${slug}-target.png`, proto=`screenshots/${batch}/${slug}-prototype.png`; if(!T.exists(target)||!T.exists(proto)) continue; const origAsset=`${assetDir}/${slug}-original.webp`, protoAsset=`${assetDir}/${slug}-prototype.webp`; makeAsset(target,origAsset); makeAsset(proto,protoAsset); const fp=`state/fidelity-repair-100/${batch}/${slug}-fingerprint.md`; const audit=`opportunities/${batch}/${slug}-audit.md`, offer=`offers/${batch}/${slug}-offer.md`, protoPath=`prototypes/${batch}/${slug}/index.html`; cards.push(`<article class="site"><div class="meta"><p class="eyebrow">${T.esc(batch)}</p><h2>${T.esc(it.name||slug)}</h2><p><code>${T.esc(slug)}</code></p><div class="links">${it.url?`<a href="${T.esc(it.url)}">Original website</a>`:''}<a href="${T.esc(page(protoPath))}">Prototype</a><a href="${T.esc(blob(audit))}">Audit report</a><a href="${T.esc(blob(offer))}">Offer letter</a>${it.contact?`<a href="${T.esc(it.contact)}">Contact point</a>`:`<a href="${T.esc(it.url||'#')}">Contact point</a>`}<a href="${T.esc(blob(fp))}">Fingerprint</a></div></div><div class="shots"><figure><img src="${T.esc(path.basename(out,'.html').startsWith('x')?raw(origAsset):`assets/${reportId}/${slug}-original.webp`)}" alt="Original website screenshot for ${T.esc(it.name||slug)}"><figcaption>Original website</figcaption></figure><figure><img src="${T.esc(`assets/${reportId}/${slug}-prototype.webp`)}" alt="Prototype screenshot for ${T.esc(it.name||slug)}"><figcaption>Prototype</figcaption></figure></div></article>`)}
const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Page Profit Check fidelity repair ${T.esc(reportId)}</title><meta name="robots" content="noindex,nofollow"><style>*{box-sizing:border-box}body{margin:0;font-family:Inter,system-ui,-apple-system,Segoe UI,Arial,sans-serif;background:#101318;color:#f4f6f8;line-height:1.5}.wrap{width:min(1500px,calc(100% - 36px));margin:auto;padding:34px 0 70px}.lede{max-width:900px;color:#c9d0d8}.eyebrow{text-transform:uppercase;letter-spacing:.12em;font-size:12px;color:#73839b;margin:0 0 6px}h1{font-size:clamp(32px,5vw,58px);line-height:1;margin:0 0 16px}h2{font-size:26px;margin:0 0 4px}.site{background:#fff;color:#17202b;border-radius:18px;padding:20px;margin:24px 0;box-shadow:0 18px 50px #0006}.meta{display:flex;align-items:end;justify-content:space-between;gap:20px;flex-wrap:wrap;margin-bottom:16px}.meta code{background:#eef2f6;padding:4px 7px;border-radius:6px}.links{display:flex;gap:10px;flex-wrap:wrap}.links a{background:#17202b;color:#fff;text-decoration:none;padding:9px 12px;border-radius:9px;font-weight:750}.shots{display:grid;grid-template-columns:1fr 1fr;gap:16px}figure{margin:0;border:1px solid #dbe1e8;border-radius:14px;overflow:hidden;background:#f6f8fa}img{display:block;width:100%;height:auto}figcaption{padding:10px 12px;font-weight:850;color:#344052}@media(max-width:900px){.shots{grid-template-columns:1fr}}</style></head><body><main class="wrap"><p class="eyebrow">Small Site Studio</p><h1>Page Profit Check visual-fidelity repair</h1><p class="lede">Lightweight side-by-side report: ${cards.length} existing opportunities, screenshots stored as separate optimized WebP assets, with inline links for each site.</p>${cards.join('\n')}</main></body></html>`;
fs.mkdirSync(path.dirname(path.join(root,out)),{recursive:true}); fs.writeFileSync(path.join(root,out),html); console.log(out, cards.length, assetDir);
